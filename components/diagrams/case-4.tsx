/**
 * 케이스 4 다이어그램 — PokeCardFlip. 아직 그리지 않았다.
 *
 * <svg> 안에 그린 뒤 hasContent를 true로 바꾸면 자리 표시자 대신 그림이 나온다.
 */
export const hasContent = false;

export function Case4Diagram() {
  return (
    <svg
      viewBox="0 0 720 400"
      className="h-auto w-full min-w-[600px] text-fg"
      aria-hidden="true"
      focusable="false"
    />
  );
}
